<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.dipanjan.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />



    <uses-permission android:name="android.permission.VIBRATE"/>


    <application
        android:name="info.dipanjan.app.app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="info.dipanjan.app.activity.MainActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
        android:name="info.dipanjan.app.activity.ListMovieItem"
            android:screenOrientation="portrait"
        android:label="@string/app_name" />

        <activity
        android:name="info.dipanjan.app.activity.MovieDeatils"
            android:screenOrientation="portrait"
            android:label=""
            />

        <activity
            android:name="info.dipanjan.app.activity.YoutubeVideoPlayer"
            android:screenOrientation="landscape"
            android:label="@string/app_name" />

        <activity
            android:name="info.dipanjan.app.activity.MovieGerneListLayout"

            android:label="@string/app_name" />

        <activity
            android:name="com.thefinestartist.finestwebview.FinestWebViewActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="sensor"
            android:theme="@style/FinestWebViewTheme.Light"/>

        <activity android:name="info.dipanjan.app.activity.WebViewActivity"/>
    </application>



</manifest>
